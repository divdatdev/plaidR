# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Total
#'
#' @description Total Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field canonical_description  \link{TotalCanonicalDescription} [optional]
#'
#' @field description  character [optional]
#'
#' @field current_pay  \link{Pay} [optional]
#'
#' @field ytd_pay  \link{Pay} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Total <- R6::R6Class(
  'Total',
  public = list(
    `canonical_description` = NULL,
    `description` = NULL,
    `current_pay` = NULL,
    `ytd_pay` = NULL,
    initialize = function(
        `canonical_description`=NULL, `description`=NULL, `current_pay`=NULL, `ytd_pay`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`canonical_description`)) {
        stopifnot(R6::is.R6(`canonical_description`))
        self$`canonical_description` <- `canonical_description`
      }
      if (!is.null(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
        self$`description` <- `description`
      }
      if (!is.null(`current_pay`)) {
        stopifnot(R6::is.R6(`current_pay`))
        self$`current_pay` <- `current_pay`
      }
      if (!is.null(`ytd_pay`)) {
        stopifnot(R6::is.R6(`ytd_pay`))
        self$`ytd_pay` <- `ytd_pay`
      }
    },
    toJSON = function() {
      TotalObject <- list()
      if (!is.null(self$`canonical_description`)) {
        TotalObject[['canonical_description']] <-
          self$`canonical_description`$toJSON()
      }
      if (!is.null(self$`description`)) {
        TotalObject[['description']] <-
          self$`description`
      }
      if (!is.null(self$`current_pay`)) {
        TotalObject[['current_pay']] <-
          self$`current_pay`$toJSON()
      }
      if (!is.null(self$`ytd_pay`)) {
        TotalObject[['ytd_pay']] <-
          self$`ytd_pay`$toJSON()
      }

      TotalObject
    },
    fromJSON = function(TotalJson) {
      TotalObject <- jsonlite::fromJSON(TotalJson)
      if (!is.null(TotalObject$`canonical_description`)) {
        canonical_descriptionObject <- TotalCanonicalDescription$new()
        canonical_descriptionObject$fromJSON(jsonlite::toJSON(TotalObject$canonical_description, auto_unbox = TRUE, digits = NA))
        self$`canonical_description` <- canonical_descriptionObject
      }
      if (!is.null(TotalObject$`description`)) {
        self$`description` <- TotalObject$`description`
      }
      if (!is.null(TotalObject$`current_pay`)) {
        current_payObject <- Pay$new()
        current_payObject$fromJSON(jsonlite::toJSON(TotalObject$current_pay, auto_unbox = TRUE, digits = NA))
        self$`current_pay` <- current_payObject
      }
      if (!is.null(TotalObject$`ytd_pay`)) {
        ytd_payObject <- Pay$new()
        ytd_payObject$fromJSON(jsonlite::toJSON(TotalObject$ytd_pay, auto_unbox = TRUE, digits = NA))
        self$`ytd_pay` <- ytd_payObject
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`canonical_description`)) {
        sprintf(
        '"canonical_description":
        %s
        ',
        jsonlite::toJSON(self$`canonical_description`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`description`)) {
        sprintf(
        '"description":
          "%s"
                ',
        self$`description`
        )},
        if (!is.null(self$`current_pay`)) {
        sprintf(
        '"current_pay":
        %s
        ',
        jsonlite::toJSON(self$`current_pay`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`ytd_pay`)) {
        sprintf(
        '"ytd_pay":
        %s
        ',
        jsonlite::toJSON(self$`ytd_pay`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(TotalJson) {
      TotalObject <- jsonlite::fromJSON(TotalJson)
      self$`canonical_description` <- TotalCanonicalDescription$new()$fromJSON(jsonlite::toJSON(TotalObject$canonical_description, auto_unbox = TRUE, digits = NA))
      self$`description` <- TotalObject$`description`
      self$`current_pay` <- Pay$new()$fromJSON(jsonlite::toJSON(TotalObject$current_pay, auto_unbox = TRUE, digits = NA))
      self$`ytd_pay` <- Pay$new()$fromJSON(jsonlite::toJSON(TotalObject$ytd_pay, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)
