# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Deductions
#'
#' @description Deductions Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field subtotals  list( \link{Total} ) [optional]
#'
#' @field breakdown  list( \link{DeductionsBreakdown} ) 
#'
#' @field totals  list( \link{Total} ) [optional]
#'
#' @field total  \link{DeductionsTotal} 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Deductions <- R6::R6Class(
  'Deductions',
  public = list(
    `subtotals` = NULL,
    `breakdown` = NULL,
    `totals` = NULL,
    `total` = NULL,
    initialize = function(
        `breakdown`, `total`, `subtotals`=NULL, `totals`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`breakdown`)) {
        stopifnot(is.vector(`breakdown`), length(`breakdown`) != 0)
        sapply(`breakdown`, function(x) stopifnot(R6::is.R6(x)))
        self$`breakdown` <- `breakdown`
      }
      if (!missing(`total`)) {
        stopifnot(R6::is.R6(`total`))
        self$`total` <- `total`
      }
      if (!is.null(`subtotals`)) {
        stopifnot(is.vector(`subtotals`), length(`subtotals`) != 0)
        sapply(`subtotals`, function(x) stopifnot(R6::is.R6(x)))
        self$`subtotals` <- `subtotals`
      }
      if (!is.null(`totals`)) {
        stopifnot(is.vector(`totals`), length(`totals`) != 0)
        sapply(`totals`, function(x) stopifnot(R6::is.R6(x)))
        self$`totals` <- `totals`
      }
    },
    toJSON = function() {
      DeductionsObject <- list()
      if (!is.null(self$`subtotals`)) {
        DeductionsObject[['subtotals']] <-
          lapply(self$`subtotals`, function(x) x$toJSON())
      }
      if (!is.null(self$`breakdown`)) {
        DeductionsObject[['breakdown']] <-
          lapply(self$`breakdown`, function(x) x$toJSON())
      }
      if (!is.null(self$`totals`)) {
        DeductionsObject[['totals']] <-
          lapply(self$`totals`, function(x) x$toJSON())
      }
      if (!is.null(self$`total`)) {
        DeductionsObject[['total']] <-
          self$`total`$toJSON()
      }

      DeductionsObject
    },
    fromJSON = function(DeductionsJson) {
      DeductionsObject <- jsonlite::fromJSON(DeductionsJson)
      if (!is.null(DeductionsObject$`subtotals`)) {
        self$`subtotals` <- ApiClient$new()$deserializeObj(DeductionsObject$`subtotals`, "array[Total]", loadNamespace("plaidr"))
      }
      if (!is.null(DeductionsObject$`breakdown`)) {
        self$`breakdown` <- ApiClient$new()$deserializeObj(DeductionsObject$`breakdown`, "array[DeductionsBreakdown]", loadNamespace("plaidr"))
      }
      if (!is.null(DeductionsObject$`totals`)) {
        self$`totals` <- ApiClient$new()$deserializeObj(DeductionsObject$`totals`, "array[Total]", loadNamespace("plaidr"))
      }
      if (!is.null(DeductionsObject$`total`)) {
        totalObject <- DeductionsTotal$new()
        totalObject$fromJSON(jsonlite::toJSON(DeductionsObject$total, auto_unbox = TRUE, digits = NA))
        self$`total` <- totalObject
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`subtotals`)) {
        sprintf(
        '"subtotals":
        [%s]
',
        paste(sapply(self$`subtotals`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`breakdown`)) {
        sprintf(
        '"breakdown":
        [%s]
',
        paste(sapply(self$`breakdown`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`totals`)) {
        sprintf(
        '"totals":
        [%s]
',
        paste(sapply(self$`totals`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`total`)) {
        sprintf(
        '"total":
        %s
        ',
        jsonlite::toJSON(self$`total`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(DeductionsJson) {
      DeductionsObject <- jsonlite::fromJSON(DeductionsJson)
      self$`subtotals` <- ApiClient$new()$deserializeObj(DeductionsObject$`subtotals`, "array[Total]", loadNamespace("plaidr"))
      self$`breakdown` <- ApiClient$new()$deserializeObj(DeductionsObject$`breakdown`, "array[DeductionsBreakdown]", loadNamespace("plaidr"))
      self$`totals` <- ApiClient$new()$deserializeObj(DeductionsObject$`totals`, "array[Total]", loadNamespace("plaidr"))
      self$`total` <- DeductionsTotal$new()$fromJSON(jsonlite::toJSON(DeductionsObject$total, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)
