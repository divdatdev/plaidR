# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title EntityScreeningHitData
#'
#' @description EntityScreeningHitData Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field documents  list( \link{EntityScreeningHitDocumentsItems} ) [optional]
#'
#' @field email_addresses  list( \link{EntityScreeningHitEmailsItems} ) [optional]
#'
#' @field locations  list( \link{GenericScreeningHitLocationItems} ) [optional]
#'
#' @field names  list( \link{EntityScreeningHitNamesItems} ) [optional]
#'
#' @field phone_numbers  list( \link{EntityScreeningHitsPhoneNumberItems} ) [optional]
#'
#' @field urls  list( \link{EntityScreeningHitUrlsItems} ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
EntityScreeningHitData <- R6::R6Class(
  'EntityScreeningHitData',
  public = list(
    `documents` = NULL,
    `email_addresses` = NULL,
    `locations` = NULL,
    `names` = NULL,
    `phone_numbers` = NULL,
    `urls` = NULL,
    initialize = function(
        `documents`=NULL, `email_addresses`=NULL, `locations`=NULL, `names`=NULL, `phone_numbers`=NULL, `urls`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`documents`)) {
        stopifnot(is.vector(`documents`), length(`documents`) != 0)
        sapply(`documents`, function(x) stopifnot(R6::is.R6(x)))
        self$`documents` <- `documents`
      }
      if (!is.null(`email_addresses`)) {
        stopifnot(is.vector(`email_addresses`), length(`email_addresses`) != 0)
        sapply(`email_addresses`, function(x) stopifnot(R6::is.R6(x)))
        self$`email_addresses` <- `email_addresses`
      }
      if (!is.null(`locations`)) {
        stopifnot(is.vector(`locations`), length(`locations`) != 0)
        sapply(`locations`, function(x) stopifnot(R6::is.R6(x)))
        self$`locations` <- `locations`
      }
      if (!is.null(`names`)) {
        stopifnot(is.vector(`names`), length(`names`) != 0)
        sapply(`names`, function(x) stopifnot(R6::is.R6(x)))
        self$`names` <- `names`
      }
      if (!is.null(`phone_numbers`)) {
        stopifnot(is.vector(`phone_numbers`), length(`phone_numbers`) != 0)
        sapply(`phone_numbers`, function(x) stopifnot(R6::is.R6(x)))
        self$`phone_numbers` <- `phone_numbers`
      }
      if (!is.null(`urls`)) {
        stopifnot(is.vector(`urls`), length(`urls`) != 0)
        sapply(`urls`, function(x) stopifnot(R6::is.R6(x)))
        self$`urls` <- `urls`
      }
    },
    toJSON = function() {
      EntityScreeningHitDataObject <- list()
      if (!is.null(self$`documents`)) {
        EntityScreeningHitDataObject[['documents']] <-
          lapply(self$`documents`, function(x) x$toJSON())
      }
      if (!is.null(self$`email_addresses`)) {
        EntityScreeningHitDataObject[['email_addresses']] <-
          lapply(self$`email_addresses`, function(x) x$toJSON())
      }
      if (!is.null(self$`locations`)) {
        EntityScreeningHitDataObject[['locations']] <-
          lapply(self$`locations`, function(x) x$toJSON())
      }
      if (!is.null(self$`names`)) {
        EntityScreeningHitDataObject[['names']] <-
          lapply(self$`names`, function(x) x$toJSON())
      }
      if (!is.null(self$`phone_numbers`)) {
        EntityScreeningHitDataObject[['phone_numbers']] <-
          lapply(self$`phone_numbers`, function(x) x$toJSON())
      }
      if (!is.null(self$`urls`)) {
        EntityScreeningHitDataObject[['urls']] <-
          lapply(self$`urls`, function(x) x$toJSON())
      }

      EntityScreeningHitDataObject
    },
    fromJSON = function(EntityScreeningHitDataJson) {
      EntityScreeningHitDataObject <- jsonlite::fromJSON(EntityScreeningHitDataJson)
      if (!is.null(EntityScreeningHitDataObject$`documents`)) {
        self$`documents` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`documents`, "array[EntityScreeningHitDocumentsItems]", loadNamespace("plaidr"))
      }
      if (!is.null(EntityScreeningHitDataObject$`email_addresses`)) {
        self$`email_addresses` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`email_addresses`, "array[EntityScreeningHitEmailsItems]", loadNamespace("plaidr"))
      }
      if (!is.null(EntityScreeningHitDataObject$`locations`)) {
        self$`locations` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`locations`, "array[GenericScreeningHitLocationItems]", loadNamespace("plaidr"))
      }
      if (!is.null(EntityScreeningHitDataObject$`names`)) {
        self$`names` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`names`, "array[EntityScreeningHitNamesItems]", loadNamespace("plaidr"))
      }
      if (!is.null(EntityScreeningHitDataObject$`phone_numbers`)) {
        self$`phone_numbers` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`phone_numbers`, "array[EntityScreeningHitsPhoneNumberItems]", loadNamespace("plaidr"))
      }
      if (!is.null(EntityScreeningHitDataObject$`urls`)) {
        self$`urls` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`urls`, "array[EntityScreeningHitUrlsItems]", loadNamespace("plaidr"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`documents`)) {
        sprintf(
        '"documents":
        [%s]
',
        paste(sapply(self$`documents`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`email_addresses`)) {
        sprintf(
        '"email_addresses":
        [%s]
',
        paste(sapply(self$`email_addresses`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`locations`)) {
        sprintf(
        '"locations":
        [%s]
',
        paste(sapply(self$`locations`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`names`)) {
        sprintf(
        '"names":
        [%s]
',
        paste(sapply(self$`names`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`phone_numbers`)) {
        sprintf(
        '"phone_numbers":
        [%s]
',
        paste(sapply(self$`phone_numbers`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`urls`)) {
        sprintf(
        '"urls":
        [%s]
',
        paste(sapply(self$`urls`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(EntityScreeningHitDataJson) {
      EntityScreeningHitDataObject <- jsonlite::fromJSON(EntityScreeningHitDataJson)
      self$`documents` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`documents`, "array[EntityScreeningHitDocumentsItems]", loadNamespace("plaidr"))
      self$`email_addresses` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`email_addresses`, "array[EntityScreeningHitEmailsItems]", loadNamespace("plaidr"))
      self$`locations` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`locations`, "array[GenericScreeningHitLocationItems]", loadNamespace("plaidr"))
      self$`names` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`names`, "array[EntityScreeningHitNamesItems]", loadNamespace("plaidr"))
      self$`phone_numbers` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`phone_numbers`, "array[EntityScreeningHitsPhoneNumberItems]", loadNamespace("plaidr"))
      self$`urls` <- ApiClient$new()$deserializeObj(EntityScreeningHitDataObject$`urls`, "array[EntityScreeningHitUrlsItems]", loadNamespace("plaidr"))
      self
    }
  )
)
