# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Earnings
#'
#' @description Earnings Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field subtotals  list( \link{EarningsTotal} ) [optional]
#'
#' @field totals  list( \link{EarningsTotal} ) [optional]
#'
#' @field breakdown  list( \link{EarningsBreakdown} ) [optional]
#'
#' @field total  \link{EarningsTotal} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Earnings <- R6::R6Class(
  'Earnings',
  public = list(
    `subtotals` = NULL,
    `totals` = NULL,
    `breakdown` = NULL,
    `total` = NULL,
    initialize = function(
        `subtotals`=NULL, `totals`=NULL, `breakdown`=NULL, `total`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`subtotals`)) {
        stopifnot(is.vector(`subtotals`), length(`subtotals`) != 0)
        sapply(`subtotals`, function(x) stopifnot(R6::is.R6(x)))
        self$`subtotals` <- `subtotals`
      }
      if (!is.null(`totals`)) {
        stopifnot(is.vector(`totals`), length(`totals`) != 0)
        sapply(`totals`, function(x) stopifnot(R6::is.R6(x)))
        self$`totals` <- `totals`
      }
      if (!is.null(`breakdown`)) {
        stopifnot(is.vector(`breakdown`), length(`breakdown`) != 0)
        sapply(`breakdown`, function(x) stopifnot(R6::is.R6(x)))
        self$`breakdown` <- `breakdown`
      }
      if (!is.null(`total`)) {
        stopifnot(R6::is.R6(`total`))
        self$`total` <- `total`
      }
    },
    toJSON = function() {
      EarningsObject <- list()
      if (!is.null(self$`subtotals`)) {
        EarningsObject[['subtotals']] <-
          lapply(self$`subtotals`, function(x) x$toJSON())
      }
      if (!is.null(self$`totals`)) {
        EarningsObject[['totals']] <-
          lapply(self$`totals`, function(x) x$toJSON())
      }
      if (!is.null(self$`breakdown`)) {
        EarningsObject[['breakdown']] <-
          lapply(self$`breakdown`, function(x) x$toJSON())
      }
      if (!is.null(self$`total`)) {
        EarningsObject[['total']] <-
          self$`total`$toJSON()
      }

      EarningsObject
    },
    fromJSON = function(EarningsJson) {
      EarningsObject <- jsonlite::fromJSON(EarningsJson)
      if (!is.null(EarningsObject$`subtotals`)) {
        self$`subtotals` <- ApiClient$new()$deserializeObj(EarningsObject$`subtotals`, "array[EarningsTotal]", loadNamespace("plaidr"))
      }
      if (!is.null(EarningsObject$`totals`)) {
        self$`totals` <- ApiClient$new()$deserializeObj(EarningsObject$`totals`, "array[EarningsTotal]", loadNamespace("plaidr"))
      }
      if (!is.null(EarningsObject$`breakdown`)) {
        self$`breakdown` <- ApiClient$new()$deserializeObj(EarningsObject$`breakdown`, "array[EarningsBreakdown]", loadNamespace("plaidr"))
      }
      if (!is.null(EarningsObject$`total`)) {
        totalObject <- EarningsTotal$new()
        totalObject$fromJSON(jsonlite::toJSON(EarningsObject$total, auto_unbox = TRUE, digits = NA))
        self$`total` <- totalObject
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`subtotals`)) {
        sprintf(
        '"subtotals":
        [%s]
',
        paste(sapply(self$`subtotals`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`totals`)) {
        sprintf(
        '"totals":
        [%s]
',
        paste(sapply(self$`totals`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`breakdown`)) {
        sprintf(
        '"breakdown":
        [%s]
',
        paste(sapply(self$`breakdown`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`total`)) {
        sprintf(
        '"total":
        %s
        ',
        jsonlite::toJSON(self$`total`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(EarningsJson) {
      EarningsObject <- jsonlite::fromJSON(EarningsJson)
      self$`subtotals` <- ApiClient$new()$deserializeObj(EarningsObject$`subtotals`, "array[EarningsTotal]", loadNamespace("plaidr"))
      self$`totals` <- ApiClient$new()$deserializeObj(EarningsObject$`totals`, "array[EarningsTotal]", loadNamespace("plaidr"))
      self$`breakdown` <- ApiClient$new()$deserializeObj(EarningsObject$`breakdown`, "array[EarningsBreakdown]", loadNamespace("plaidr"))
      self$`total` <- EarningsTotal$new()$fromJSON(jsonlite::toJSON(EarningsObject$total, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)
