# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PaymentInitiationAddress
#'
#' @description PaymentInitiationAddress Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field street  list( character ) 
#'
#' @field city  character 
#'
#' @field postal_code  character 
#'
#' @field country  character 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PaymentInitiationAddress <- R6::R6Class(
  'PaymentInitiationAddress',
  public = list(
    `street` = NULL,
    `city` = NULL,
    `postal_code` = NULL,
    `country` = NULL,
    initialize = function(
        `street`, `city`, `postal_code`, `country`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`street`)) {
        stopifnot(is.vector(`street`), length(`street`) != 0)
        sapply(`street`, function(x) stopifnot(is.character(x)))
        self$`street` <- `street`
      }
      if (!missing(`city`)) {
        stopifnot(is.character(`city`), length(`city`) == 1)
        self$`city` <- `city`
      }
      if (!missing(`postal_code`)) {
        stopifnot(is.character(`postal_code`), length(`postal_code`) == 1)
        self$`postal_code` <- `postal_code`
      }
      if (!missing(`country`)) {
        stopifnot(is.character(`country`), length(`country`) == 1)
        self$`country` <- `country`
      }
    },
    toJSON = function() {
      PaymentInitiationAddressObject <- list()
      if (!is.null(self$`street`)) {
        PaymentInitiationAddressObject[['street']] <-
          self$`street`
      }
      if (!is.null(self$`city`)) {
        PaymentInitiationAddressObject[['city']] <-
          self$`city`
      }
      if (!is.null(self$`postal_code`)) {
        PaymentInitiationAddressObject[['postal_code']] <-
          self$`postal_code`
      }
      if (!is.null(self$`country`)) {
        PaymentInitiationAddressObject[['country']] <-
          self$`country`
      }

      PaymentInitiationAddressObject
    },
    fromJSON = function(PaymentInitiationAddressJson) {
      PaymentInitiationAddressObject <- jsonlite::fromJSON(PaymentInitiationAddressJson)
      if (!is.null(PaymentInitiationAddressObject$`street`)) {
        self$`street` <- ApiClient$new()$deserializeObj(PaymentInitiationAddressObject$`street`, "array[character]", loadNamespace("plaidr"))
      }
      if (!is.null(PaymentInitiationAddressObject$`city`)) {
        self$`city` <- PaymentInitiationAddressObject$`city`
      }
      if (!is.null(PaymentInitiationAddressObject$`postal_code`)) {
        self$`postal_code` <- PaymentInitiationAddressObject$`postal_code`
      }
      if (!is.null(PaymentInitiationAddressObject$`country`)) {
        self$`country` <- PaymentInitiationAddressObject$`country`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`street`)) {
        sprintf(
        '"street":
           [%s]
        ',
        paste(unlist(lapply(self$`street`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`city`)) {
        sprintf(
        '"city":
          "%s"
                ',
        self$`city`
        )},
        if (!is.null(self$`postal_code`)) {
        sprintf(
        '"postal_code":
          "%s"
                ',
        self$`postal_code`
        )},
        if (!is.null(self$`country`)) {
        sprintf(
        '"country":
          "%s"
                ',
        self$`country`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(PaymentInitiationAddressJson) {
      PaymentInitiationAddressObject <- jsonlite::fromJSON(PaymentInitiationAddressJson)
      self$`street` <- ApiClient$new()$deserializeObj(PaymentInitiationAddressObject$`street`, "array[character]", loadNamespace("plaidr"))
      self$`city` <- PaymentInitiationAddressObject$`city`
      self$`postal_code` <- PaymentInitiationAddressObject$`postal_code`
      self$`country` <- PaymentInitiationAddressObject$`country`
      self
    }
  )
)
