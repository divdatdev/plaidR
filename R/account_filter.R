# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title AccountFilter
#'
#' @description AccountFilter Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field depository  list( character ) [optional]
#'
#' @field credit  list( character ) [optional]
#'
#' @field loan  list( character ) [optional]
#'
#' @field investment  list( character ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
AccountFilter <- R6::R6Class(
  'AccountFilter',
  public = list(
    `depository` = NULL,
    `credit` = NULL,
    `loan` = NULL,
    `investment` = NULL,
    initialize = function(
        `depository`=NULL, `credit`=NULL, `loan`=NULL, `investment`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`depository`)) {
        stopifnot(is.vector(`depository`), length(`depository`) != 0)
        sapply(`depository`, function(x) stopifnot(is.character(x)))
        self$`depository` <- `depository`
      }
      if (!is.null(`credit`)) {
        stopifnot(is.vector(`credit`), length(`credit`) != 0)
        sapply(`credit`, function(x) stopifnot(is.character(x)))
        self$`credit` <- `credit`
      }
      if (!is.null(`loan`)) {
        stopifnot(is.vector(`loan`), length(`loan`) != 0)
        sapply(`loan`, function(x) stopifnot(is.character(x)))
        self$`loan` <- `loan`
      }
      if (!is.null(`investment`)) {
        stopifnot(is.vector(`investment`), length(`investment`) != 0)
        sapply(`investment`, function(x) stopifnot(is.character(x)))
        self$`investment` <- `investment`
      }
    },
    toJSON = function() {
      AccountFilterObject <- list()
      if (!is.null(self$`depository`)) {
        AccountFilterObject[['depository']] <-
          self$`depository`
      }
      if (!is.null(self$`credit`)) {
        AccountFilterObject[['credit']] <-
          self$`credit`
      }
      if (!is.null(self$`loan`)) {
        AccountFilterObject[['loan']] <-
          self$`loan`
      }
      if (!is.null(self$`investment`)) {
        AccountFilterObject[['investment']] <-
          self$`investment`
      }

      AccountFilterObject
    },
    fromJSON = function(AccountFilterJson) {
      AccountFilterObject <- jsonlite::fromJSON(AccountFilterJson)
      if (!is.null(AccountFilterObject$`depository`)) {
        self$`depository` <- ApiClient$new()$deserializeObj(AccountFilterObject$`depository`, "array[character]", loadNamespace("plaidr"))
      }
      if (!is.null(AccountFilterObject$`credit`)) {
        self$`credit` <- ApiClient$new()$deserializeObj(AccountFilterObject$`credit`, "array[character]", loadNamespace("plaidr"))
      }
      if (!is.null(AccountFilterObject$`loan`)) {
        self$`loan` <- ApiClient$new()$deserializeObj(AccountFilterObject$`loan`, "array[character]", loadNamespace("plaidr"))
      }
      if (!is.null(AccountFilterObject$`investment`)) {
        self$`investment` <- ApiClient$new()$deserializeObj(AccountFilterObject$`investment`, "array[character]", loadNamespace("plaidr"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`depository`)) {
        sprintf(
        '"depository":
           [%s]
        ',
        paste(unlist(lapply(self$`depository`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`credit`)) {
        sprintf(
        '"credit":
           [%s]
        ',
        paste(unlist(lapply(self$`credit`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`loan`)) {
        sprintf(
        '"loan":
           [%s]
        ',
        paste(unlist(lapply(self$`loan`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`investment`)) {
        sprintf(
        '"investment":
           [%s]
        ',
        paste(unlist(lapply(self$`investment`, function(x) paste0('"', x, '"'))), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(AccountFilterJson) {
      AccountFilterObject <- jsonlite::fromJSON(AccountFilterJson)
      self$`depository` <- ApiClient$new()$deserializeObj(AccountFilterObject$`depository`, "array[character]", loadNamespace("plaidr"))
      self$`credit` <- ApiClient$new()$deserializeObj(AccountFilterObject$`credit`, "array[character]", loadNamespace("plaidr"))
      self$`loan` <- ApiClient$new()$deserializeObj(AccountFilterObject$`loan`, "array[character]", loadNamespace("plaidr"))
      self$`investment` <- ApiClient$new()$deserializeObj(AccountFilterObject$`investment`, "array[character]", loadNamespace("plaidr"))
      self
    }
  )
)
