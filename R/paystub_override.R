# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title PaystubOverride
#'
#' @description PaystubOverride Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field employer  \link{PaystubOverrideEmployer} [optional]
#'
#' @field employee  \link{PaystubOverrideEmployee} [optional]
#'
#' @field income_breakdown  list( \link{IncomeBreakdown} ) [optional]
#'
#' @field pay_period_details  \link{PayPeriodDetails} [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
PaystubOverride <- R6::R6Class(
  'PaystubOverride',
  public = list(
    `employer` = NULL,
    `employee` = NULL,
    `income_breakdown` = NULL,
    `pay_period_details` = NULL,
    initialize = function(
        `employer`=NULL, `employee`=NULL, `income_breakdown`=NULL, `pay_period_details`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`employer`)) {
        stopifnot(R6::is.R6(`employer`))
        self$`employer` <- `employer`
      }
      if (!is.null(`employee`)) {
        stopifnot(R6::is.R6(`employee`))
        self$`employee` <- `employee`
      }
      if (!is.null(`income_breakdown`)) {
        stopifnot(is.vector(`income_breakdown`), length(`income_breakdown`) != 0)
        sapply(`income_breakdown`, function(x) stopifnot(R6::is.R6(x)))
        self$`income_breakdown` <- `income_breakdown`
      }
      if (!is.null(`pay_period_details`)) {
        stopifnot(R6::is.R6(`pay_period_details`))
        self$`pay_period_details` <- `pay_period_details`
      }
    },
    toJSON = function() {
      PaystubOverrideObject <- list()
      if (!is.null(self$`employer`)) {
        PaystubOverrideObject[['employer']] <-
          self$`employer`$toJSON()
      }
      if (!is.null(self$`employee`)) {
        PaystubOverrideObject[['employee']] <-
          self$`employee`$toJSON()
      }
      if (!is.null(self$`income_breakdown`)) {
        PaystubOverrideObject[['income_breakdown']] <-
          lapply(self$`income_breakdown`, function(x) x$toJSON())
      }
      if (!is.null(self$`pay_period_details`)) {
        PaystubOverrideObject[['pay_period_details']] <-
          self$`pay_period_details`$toJSON()
      }

      PaystubOverrideObject
    },
    fromJSON = function(PaystubOverrideJson) {
      PaystubOverrideObject <- jsonlite::fromJSON(PaystubOverrideJson)
      if (!is.null(PaystubOverrideObject$`employer`)) {
        employerObject <- PaystubOverrideEmployer$new()
        employerObject$fromJSON(jsonlite::toJSON(PaystubOverrideObject$employer, auto_unbox = TRUE, digits = NA))
        self$`employer` <- employerObject
      }
      if (!is.null(PaystubOverrideObject$`employee`)) {
        employeeObject <- PaystubOverrideEmployee$new()
        employeeObject$fromJSON(jsonlite::toJSON(PaystubOverrideObject$employee, auto_unbox = TRUE, digits = NA))
        self$`employee` <- employeeObject
      }
      if (!is.null(PaystubOverrideObject$`income_breakdown`)) {
        self$`income_breakdown` <- ApiClient$new()$deserializeObj(PaystubOverrideObject$`income_breakdown`, "array[IncomeBreakdown]", loadNamespace("plaidr"))
      }
      if (!is.null(PaystubOverrideObject$`pay_period_details`)) {
        pay_period_detailsObject <- PayPeriodDetails$new()
        pay_period_detailsObject$fromJSON(jsonlite::toJSON(PaystubOverrideObject$pay_period_details, auto_unbox = TRUE, digits = NA))
        self$`pay_period_details` <- pay_period_detailsObject
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`employer`)) {
        sprintf(
        '"employer":
        %s
        ',
        jsonlite::toJSON(self$`employer`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`employee`)) {
        sprintf(
        '"employee":
        %s
        ',
        jsonlite::toJSON(self$`employee`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`income_breakdown`)) {
        sprintf(
        '"income_breakdown":
        [%s]
',
        paste(sapply(self$`income_breakdown`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`pay_period_details`)) {
        sprintf(
        '"pay_period_details":
        %s
        ',
        jsonlite::toJSON(self$`pay_period_details`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(PaystubOverrideJson) {
      PaystubOverrideObject <- jsonlite::fromJSON(PaystubOverrideJson)
      self$`employer` <- PaystubOverrideEmployer$new()$fromJSON(jsonlite::toJSON(PaystubOverrideObject$employer, auto_unbox = TRUE, digits = NA))
      self$`employee` <- PaystubOverrideEmployee$new()$fromJSON(jsonlite::toJSON(PaystubOverrideObject$employee, auto_unbox = TRUE, digits = NA))
      self$`income_breakdown` <- ApiClient$new()$deserializeObj(PaystubOverrideObject$`income_breakdown`, "array[IncomeBreakdown]", loadNamespace("plaidr"))
      self$`pay_period_details` <- PayPeriodDetails$new()$fromJSON(jsonlite::toJSON(PaystubOverrideObject$pay_period_details, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)
