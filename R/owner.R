# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Owner
#'
#' @description Owner Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field names  list( character ) 
#'
#' @field phone_numbers  list( \link{PhoneNumber} ) 
#'
#' @field emails  list( \link{Email} ) 
#'
#' @field addresses  list( \link{Address} ) 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Owner <- R6::R6Class(
  'Owner',
  public = list(
    `names` = NULL,
    `phone_numbers` = NULL,
    `emails` = NULL,
    `addresses` = NULL,
    initialize = function(
        `names`, `phone_numbers`, `emails`, `addresses`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`names`)) {
        stopifnot(is.vector(`names`), length(`names`) != 0)
        sapply(`names`, function(x) stopifnot(is.character(x)))
        self$`names` <- `names`
      }
      if (!missing(`phone_numbers`)) {
        stopifnot(is.vector(`phone_numbers`), length(`phone_numbers`) != 0)
        sapply(`phone_numbers`, function(x) stopifnot(R6::is.R6(x)))
        self$`phone_numbers` <- `phone_numbers`
      }
      if (!missing(`emails`)) {
        stopifnot(is.vector(`emails`), length(`emails`) != 0)
        sapply(`emails`, function(x) stopifnot(R6::is.R6(x)))
        self$`emails` <- `emails`
      }
      if (!missing(`addresses`)) {
        stopifnot(is.vector(`addresses`), length(`addresses`) != 0)
        sapply(`addresses`, function(x) stopifnot(R6::is.R6(x)))
        self$`addresses` <- `addresses`
      }
    },
    toJSON = function() {
      OwnerObject <- list()
      if (!is.null(self$`names`)) {
        OwnerObject[['names']] <-
          self$`names`
      }
      if (!is.null(self$`phone_numbers`)) {
        OwnerObject[['phone_numbers']] <-
          lapply(self$`phone_numbers`, function(x) x$toJSON())
      }
      if (!is.null(self$`emails`)) {
        OwnerObject[['emails']] <-
          lapply(self$`emails`, function(x) x$toJSON())
      }
      if (!is.null(self$`addresses`)) {
        OwnerObject[['addresses']] <-
          lapply(self$`addresses`, function(x) x$toJSON())
      }

      OwnerObject
    },
    fromJSON = function(OwnerJson) {
      OwnerObject <- jsonlite::fromJSON(OwnerJson)
      if (!is.null(OwnerObject$`names`)) {
        self$`names` <- ApiClient$new()$deserializeObj(OwnerObject$`names`, "array[character]", loadNamespace("plaidr"))
      }
      if (!is.null(OwnerObject$`phone_numbers`)) {
        self$`phone_numbers` <- ApiClient$new()$deserializeObj(OwnerObject$`phone_numbers`, "array[PhoneNumber]", loadNamespace("plaidr"))
      }
      if (!is.null(OwnerObject$`emails`)) {
        self$`emails` <- ApiClient$new()$deserializeObj(OwnerObject$`emails`, "array[Email]", loadNamespace("plaidr"))
      }
      if (!is.null(OwnerObject$`addresses`)) {
        self$`addresses` <- ApiClient$new()$deserializeObj(OwnerObject$`addresses`, "array[Address]", loadNamespace("plaidr"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`names`)) {
        sprintf(
        '"names":
           [%s]
        ',
        paste(unlist(lapply(self$`names`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`phone_numbers`)) {
        sprintf(
        '"phone_numbers":
        [%s]
',
        paste(sapply(self$`phone_numbers`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`emails`)) {
        sprintf(
        '"emails":
        [%s]
',
        paste(sapply(self$`emails`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`addresses`)) {
        sprintf(
        '"addresses":
        [%s]
',
        paste(sapply(self$`addresses`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(OwnerJson) {
      OwnerObject <- jsonlite::fromJSON(OwnerJson)
      self$`names` <- ApiClient$new()$deserializeObj(OwnerObject$`names`, "array[character]", loadNamespace("plaidr"))
      self$`phone_numbers` <- ApiClient$new()$deserializeObj(OwnerObject$`phone_numbers`, "array[PhoneNumber]", loadNamespace("plaidr"))
      self$`emails` <- ApiClient$new()$deserializeObj(OwnerObject$`emails`, "array[Email]", loadNamespace("plaidr"))
      self$`addresses` <- ApiClient$new()$deserializeObj(OwnerObject$`addresses`, "array[Address]", loadNamespace("plaidr"))
      self
    }
  )
)
