# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title HealthIncident
#'
#' @description HealthIncident Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field start_date  character 
#'
#' @field end_date  character [optional]
#'
#' @field title  character 
#'
#' @field incident_updates  list( \link{IncidentUpdate} ) 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
HealthIncident <- R6::R6Class(
  'HealthIncident',
  public = list(
    `start_date` = NULL,
    `end_date` = NULL,
    `title` = NULL,
    `incident_updates` = NULL,
    initialize = function(
        `start_date`, `title`, `incident_updates`, `end_date`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`start_date`)) {
        stopifnot(is.character(`start_date`), length(`start_date`) == 1)
        self$`start_date` <- `start_date`
      }
      if (!missing(`title`)) {
        stopifnot(is.character(`title`), length(`title`) == 1)
        self$`title` <- `title`
      }
      if (!missing(`incident_updates`)) {
        stopifnot(is.vector(`incident_updates`), length(`incident_updates`) != 0)
        sapply(`incident_updates`, function(x) stopifnot(R6::is.R6(x)))
        self$`incident_updates` <- `incident_updates`
      }
      if (!is.null(`end_date`)) {
        stopifnot(is.character(`end_date`), length(`end_date`) == 1)
        self$`end_date` <- `end_date`
      }
    },
    toJSON = function() {
      HealthIncidentObject <- list()
      if (!is.null(self$`start_date`)) {
        HealthIncidentObject[['start_date']] <-
          self$`start_date`
      }
      if (!is.null(self$`end_date`)) {
        HealthIncidentObject[['end_date']] <-
          self$`end_date`
      }
      if (!is.null(self$`title`)) {
        HealthIncidentObject[['title']] <-
          self$`title`
      }
      if (!is.null(self$`incident_updates`)) {
        HealthIncidentObject[['incident_updates']] <-
          lapply(self$`incident_updates`, function(x) x$toJSON())
      }

      HealthIncidentObject
    },
    fromJSON = function(HealthIncidentJson) {
      HealthIncidentObject <- jsonlite::fromJSON(HealthIncidentJson)
      if (!is.null(HealthIncidentObject$`start_date`)) {
        self$`start_date` <- HealthIncidentObject$`start_date`
      }
      if (!is.null(HealthIncidentObject$`end_date`)) {
        self$`end_date` <- HealthIncidentObject$`end_date`
      }
      if (!is.null(HealthIncidentObject$`title`)) {
        self$`title` <- HealthIncidentObject$`title`
      }
      if (!is.null(HealthIncidentObject$`incident_updates`)) {
        self$`incident_updates` <- ApiClient$new()$deserializeObj(HealthIncidentObject$`incident_updates`, "array[IncidentUpdate]", loadNamespace("plaidr"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`start_date`)) {
        sprintf(
        '"start_date":
          "%s"
                ',
        self$`start_date`
        )},
        if (!is.null(self$`end_date`)) {
        sprintf(
        '"end_date":
          "%s"
                ',
        self$`end_date`
        )},
        if (!is.null(self$`title`)) {
        sprintf(
        '"title":
          "%s"
                ',
        self$`title`
        )},
        if (!is.null(self$`incident_updates`)) {
        sprintf(
        '"incident_updates":
        [%s]
',
        paste(sapply(self$`incident_updates`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(HealthIncidentJson) {
      HealthIncidentObject <- jsonlite::fromJSON(HealthIncidentJson)
      self$`start_date` <- HealthIncidentObject$`start_date`
      self$`end_date` <- HealthIncidentObject$`end_date`
      self$`title` <- HealthIncidentObject$`title`
      self$`incident_updates` <- ApiClient$new()$deserializeObj(HealthIncidentObject$`incident_updates`, "array[IncidentUpdate]", loadNamespace("plaidr"))
      self
    }
  )
)
