# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title APR
#'
#' @description APR Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field apr_percentage  numeric 
#'
#' @field apr_type  character 
#'
#' @field balance_subject_to_apr  numeric 
#'
#' @field interest_charge_amount  numeric 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
APR <- R6::R6Class(
  'APR',
  public = list(
    `apr_percentage` = NULL,
    `apr_type` = NULL,
    `balance_subject_to_apr` = NULL,
    `interest_charge_amount` = NULL,
    initialize = function(
        `apr_percentage`, `apr_type`, `balance_subject_to_apr`, `interest_charge_amount`, ...
    ) {
      local.optional.var <- list(...)
      if (!missing(`apr_percentage`)) {
        stopifnot(is.numeric(`apr_percentage`), length(`apr_percentage`) == 1)
        self$`apr_percentage` <- `apr_percentage`
      }
      if (!missing(`apr_type`)) {
        stopifnot(is.character(`apr_type`), length(`apr_type`) == 1)
        self$`apr_type` <- `apr_type`
      }
      if (!missing(`balance_subject_to_apr`)) {
        stopifnot(is.numeric(`balance_subject_to_apr`), length(`balance_subject_to_apr`) == 1)
        self$`balance_subject_to_apr` <- `balance_subject_to_apr`
      }
      if (!missing(`interest_charge_amount`)) {
        stopifnot(is.numeric(`interest_charge_amount`), length(`interest_charge_amount`) == 1)
        self$`interest_charge_amount` <- `interest_charge_amount`
      }
    },
    toJSON = function() {
      APRObject <- list()
      if (!is.null(self$`apr_percentage`)) {
        APRObject[['apr_percentage']] <-
          self$`apr_percentage`
      }
      if (!is.null(self$`apr_type`)) {
        APRObject[['apr_type']] <-
          self$`apr_type`
      }
      if (!is.null(self$`balance_subject_to_apr`)) {
        APRObject[['balance_subject_to_apr']] <-
          self$`balance_subject_to_apr`
      }
      if (!is.null(self$`interest_charge_amount`)) {
        APRObject[['interest_charge_amount']] <-
          self$`interest_charge_amount`
      }

      APRObject
    },
    fromJSON = function(APRJson) {
      APRObject <- jsonlite::fromJSON(APRJson)
      if (!is.null(APRObject$`apr_percentage`)) {
        self$`apr_percentage` <- APRObject$`apr_percentage`
      }
      if (!is.null(APRObject$`apr_type`)) {
        self$`apr_type` <- APRObject$`apr_type`
      }
      if (!is.null(APRObject$`balance_subject_to_apr`)) {
        self$`balance_subject_to_apr` <- APRObject$`balance_subject_to_apr`
      }
      if (!is.null(APRObject$`interest_charge_amount`)) {
        self$`interest_charge_amount` <- APRObject$`interest_charge_amount`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`apr_percentage`)) {
        sprintf(
        '"apr_percentage":
          %d
                ',
        self$`apr_percentage`
        )},
        if (!is.null(self$`apr_type`)) {
        sprintf(
        '"apr_type":
          "%s"
                ',
        self$`apr_type`
        )},
        if (!is.null(self$`balance_subject_to_apr`)) {
        sprintf(
        '"balance_subject_to_apr":
          %d
                ',
        self$`balance_subject_to_apr`
        )},
        if (!is.null(self$`interest_charge_amount`)) {
        sprintf(
        '"interest_charge_amount":
          %d
                ',
        self$`interest_charge_amount`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(APRJson) {
      APRObject <- jsonlite::fromJSON(APRJson)
      self$`apr_percentage` <- APRObject$`apr_percentage`
      self$`apr_type` <- APRObject$`apr_type`
      self$`balance_subject_to_apr` <- APRObject$`balance_subject_to_apr`
      self$`interest_charge_amount` <- APRObject$`interest_charge_amount`
      self
    }
  )
)
