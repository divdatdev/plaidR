# The Plaid API
#
# The Plaid REST API. Please see https://plaid.com/docs/api for more details.
#
# The version of the OpenAPI document: 2020-09-14_1.146.0
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title IncomeOverride
#'
#' @description IncomeOverride Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field paystubs  list( \link{PaystubOverride} ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
IncomeOverride <- R6::R6Class(
  'IncomeOverride',
  public = list(
    `paystubs` = NULL,
    initialize = function(
        `paystubs`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`paystubs`)) {
        stopifnot(is.vector(`paystubs`), length(`paystubs`) != 0)
        sapply(`paystubs`, function(x) stopifnot(R6::is.R6(x)))
        self$`paystubs` <- `paystubs`
      }
    },
    toJSON = function() {
      IncomeOverrideObject <- list()
      if (!is.null(self$`paystubs`)) {
        IncomeOverrideObject[['paystubs']] <-
          lapply(self$`paystubs`, function(x) x$toJSON())
      }

      IncomeOverrideObject
    },
    fromJSON = function(IncomeOverrideJson) {
      IncomeOverrideObject <- jsonlite::fromJSON(IncomeOverrideJson)
      if (!is.null(IncomeOverrideObject$`paystubs`)) {
        self$`paystubs` <- ApiClient$new()$deserializeObj(IncomeOverrideObject$`paystubs`, "array[PaystubOverride]", loadNamespace("plaidr"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`paystubs`)) {
        sprintf(
        '"paystubs":
        [%s]
',
        paste(sapply(self$`paystubs`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(IncomeOverrideJson) {
      IncomeOverrideObject <- jsonlite::fromJSON(IncomeOverrideJson)
      self$`paystubs` <- ApiClient$new()$deserializeObj(IncomeOverrideObject$`paystubs`, "array[PaystubOverride]", loadNamespace("plaidr"))
      self
    }
  )
)
